# Override for local dev without self-hosted Piped.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up api

services:
  api:
    environment:
      PIPED_BASE_URL: https://pipedapi.kavin.rocks
    depends_on: []
